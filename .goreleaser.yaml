before:
  hooks:
    - go mod tidy
builds:
  - env:
      - CGO_ENABLED=0
    targets:
      - linux_amd64
    binary: cuegen
    ldflags:
      - "-s -w -X github.com/noris-network/cuegen/internal/app.build={{.Version}}"
checksum:
  name_template: "checksums.txt"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
